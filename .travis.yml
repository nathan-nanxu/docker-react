language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t nathannanxu/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nathannanxu/docker-react npm run test
